import { View, Text } from 'react-native';
export default function Verify() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Verification Screen (Upload ID, AI + KYC)</Text>
    </View>
  );
}